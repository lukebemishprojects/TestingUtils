plugins {
    id 'java'
}

java.registerFeature('platformTest') {
    usingSourceSet(sourceSets.create('platformTest'))
    disablePublication()
}

testingUtils.makeTestingEnvironment(sourceSets.platformTest) {
    testPackages.add('dev.lukebemish.testingutils.testsubproject')
    modular = true
}
